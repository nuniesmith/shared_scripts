#!/bin/bash
# (Relocated) Multi-server start script (trimmed). Use orchestration logic.
echo "[INFO] multi-start relocated from start-multi.sh; prefer bin/fks or orchestration/start.sh with appropriate flags.";
