#!/bin/bash
# (Relocated) Multi-server stop script (trimmed). Use orchestration logic.
echo "[INFO] multi-stop relocated from stop-multi.sh; prefer bin/fks or orchestration/stop.sh with appropriate flags.";
